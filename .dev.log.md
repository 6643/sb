# Development Log

## 2026-03-03
- **解析与生成重构**: 解析链路完成扁平化整理，错误分支改为卫语句优先返回，降低嵌套层级。
- **协议一致性修复**: Go/TS `bool[]` 编解码统一为位图协议；补充长度上限与读取边界校验。
- **语法收敛**: 关键符号缺失时立即失败并返回行号，禁止 API 参数尾随逗号。
- **生成后端增强**: 增加双后端能力。
  - `tpl`: 模板后端（默认，完整输出）。
  - `prog`: 程序化后端（调试/实验用途）。
- **目录收敛**: 删除历史版本目录，主实现提升到根目录。
  - 根入口：`main.go`
  - 核心包：`internal/*`
  - 导入路径统一为：`sb/internal/...`
- **文档更新**: `README.md` 补充 `-backend` 参数说明。
- **验证结果**:
  - `go test ./...` 通过。
  - `go run . -backend tpl -go ./go -ts ./ts -tag bson,json aaa.sb` 通过。
  - `go run . -backend prog -go /tmp/sb-root-promoted-prog/go -ts /tmp/sb-root-promoted-prog/ts -tag bson,json aaa.sb` 通过。
- **internal 目录再扁平化**:
  - `internal/codegen/gogen -> internal/gogen`
  - `internal/codegen/tplgen -> internal/tplgen`
  - `internal/codegen/tsgen -> internal/tsgen`
  - `main.go` 导入路径同步为 `sb/internal/{gogen,tplgen,tsgen}`。
- **验证结果（当前容器）**:
  - 文本扫描确认旧路径 `sb/internal/codegen/{gogen,tplgen,tsgen}` 已清零。
  - 当前环境缺少 `go`/`gofmt`，无法在容器内执行 `go test ./...` 与格式化。
- **校验补充（2026-03-03）**:
  - 使用本机 Go 1.26.0（`/home/_/_/go/bin/go`）执行 `go test ./...`：通过。
  - 执行 `go run . -backend tpl ... aaa.sb`：通过。
  - 执行 `go run . -backend prog ... aaa.sb`：通过。
  - `gofmt -l` 发现未格式化文件集中在 `go/sb/*.go`（生成产物），源码目录 `main.go` 与 `internal/*` 已可通过编译校验。
- **校验补充（继续）**:
  - 已执行 `gofmt -w go/sb/*.go`，随后 `gofmt -l go/sb/*.go` 无输出。
  - 再次执行 `go test ./...`：通过。
  - 再次执行 `go run . -backend tpl ... aaa.sb`：通过。
  - 再次执行 `go run . -backend prog ... aaa.sb`：通过。
- **生成入口收敛（移除 backend 开关）**:
  - 删除 CLI 参数 `-backend`，入口固定走模板后端 `tplgen.Generate`。
  - 删除 `main.go` 中 `prog` 分支调用路径（`gogen.Generate` / `tsgen.Generate` / `writeDocs`）。
  - `README.md` 已移除 `-backend` 参数说明，示例命令同步为无 `-backend` 版本。
- **验证结果（移除 backend 后）**:
  - `gofmt -w main.go` 已执行。
  - `go test ./...` 通过。
  - `go run . -go /tmp/sb-validate-no-backend/go -ts /tmp/sb-validate-no-backend/ts -tag bson,json aaa.sb` 通过。
  - `go run . -backend tpl aaa.sb` 预期失败：`flag provided but not defined: -backend`。
- **短路径收敛（codegen -> gen）**:
  - 目录改名：`internal/codegen -> internal/gen`。
  - 包名改名：`package codegen -> package gen`。
  - 导入路径统一为：`sb/internal/gen`，配置类型引用统一为：`gen.Config`。
- **验证结果（短路径收敛后）**:
  - `gofmt` 已执行（`main.go`、`internal/gen/*`、相关生成器文件）。
  - `go test ./...` 通过（含 `sb/internal/gen`）。
  - `go run . -go /tmp/sb-validate-short-path/go -ts /tmp/sb-validate-short-path/ts -tag bson,json aaa.sb` 通过。
- **API 手改文件保护（tpl 后端）**:
  - `api.*.go` 生成文件增加首行指纹：`// sbgen:fingerprint <sha256>`。
  - 已存在文件处理策略：
    - 不存在：直接生成。
    - 存在且无指纹但内容等于当前生成内容：自动升级为带指纹版本。
    - 存在且有指纹且内容哈希匹配：判定为未手改，可按最新模板/Schema 自动更新。
    - 其余情况：判定为手改，跳过覆盖。
- **验证结果（API 手改保护）**:
  - `go test ./...` 通过。
  - `go run . -go /tmp/sb-fingerprint/go -ts /tmp/sb-fingerprint/ts -tag bson,json aaa.sb` 通过。
  - 手动修改 `/tmp/sb-fingerprint/go/sb/api.get_bin.go` 后再次生成，手改内容保留。
  - 将 `/tmp/sb-fingerprint/go/sb/api.get_count.go` 模拟为旧版无指纹文件后再次生成，自动升级为带指纹版本。
- **API 目录拆分（自动/手动分离）**:
  - 自动生成模型与编解码继续输出到 `go/sb`。
  - API 手动实现文件改为输出到 `go/api`（`api.*.go`）。
  - 路由与 handler 聚合文件 `api._.go` 改为输出到 `go/api`。
  - 生成时会迁移旧位置文件：`go/sb/api.<name>.go -> go/api/api.<name>.go`，并删除旧的 `go/sb/api._.go`。
- **验证结果（目录拆分后）**:
  - `go run . -go ./go -ts ./ts -tag bson,json aaa.sb` 通过。
  - 目录确认：`go/sb` 不再包含 `api.*.go`；`go/api` 包含 `api._.go` 与 `api.<name>.go`。
  - `go test ./...` 通过（含 `sb/go/api`）。
- **路由注册命名收敛**:
  - `user.xxx` 分组注册函数由 `RegisterUser` 改为 `RegisterUserApi`。
  - 默认分组保留 `RegisterApi`（避免 `RegisterApiApi` 重复后缀）。
- **验证结果（命名收敛后）**:
  - 生成结果确认：`go/api/api._.go` 中存在 `RegisterApi` 与 `RegisterUserApi`。
  - 使用 Go 1.26.0（`/home/_/.local/share/Trash/files/go1.26.0.linux-amd64/go`）执行 `go test ./...`：通过。
- **目录策略回退（合并回 sb 单目录）**:
  - 取消 `go/api` 作为生成目标，`api.*.go` 与 `api._.go` 统一回写到 `go/sb`。
  - 保留注册函数命名规则：默认分组 `RegisterApi`，命名空间分组如 `user.xxx` 为 `RegisterUserApi`。
  - 增加迁移：若历史存在 `go/api/api.*.go`，生成时自动迁移到 `go/sb`（同名已存在时仅清理重复文件，不覆盖差异文件）。
- **验证结果（合并回单目录后）**:
  - `go run . -go ./go -ts ./ts -tag bson,json aaa.sb` 通过。
  - 目录确认：`go/sb` 包含 `api._.go` 与 `api.*.go`；`go/api` 已移除。
  - `go test ./...` 通过。
- **tpl Go 生成器拆分（职责扁平化）**:
  - `internal/tplgen/go_generator.go` 精简为结构体定义与 `FuncMap` 初始化。
  - 新增 `internal/tplgen/go_rules.go`：类型映射、零值、Tag 规则。
  - 新增 `internal/tplgen/go_generate_flow.go`：`Generate` 主流程与文件编排。
  - 新增 `internal/tplgen/go_file_policy.go`：`go/api -> go/sb` 迁移策略。
  - 新增 `internal/tplgen/go_template_render.go`：模板渲染与缓存。
  - 新增 `internal/tplgen/go_fingerprint.go`：指纹写入与对比函数。
- **验证结果（生成器拆分后）**:
  - `go test ./...` 通过。
  - `go run . -go /tmp/sb-split-check/go -ts /tmp/sb-split-check/ts -tag bson,json aaa.sb` 通过。
- **gen 目录纠偏（仅迁移 gogen/tsgen 代码目录）**:
  - 回退误改：生成产物目录恢复为 `go/` 与 `ts/`，默认参数恢复为 `-go ./go`、`-ts ./ts`。
  - 代码迁移：`internal/gogen -> internal/gen/gogen`，`internal/tsgen -> internal/gen/tsgen`。
  - 文档同步：`README.md` 示例与目录说明已恢复为 `go/sb` 路径。
- **验证结果（纠偏后）**:
  - `gofmt` 已执行：`/home/_/_/go/bin/gofmt -w main.go internal/gen/config.go internal/gen/gogen/generator.go internal/gen/tsgen/generator.go internal/tplgen/*.go`。
  - `GOCACHE=/tmp/go-build /home/_/_/go/bin/go test ./...` 通过。
  - `GOCACHE=/tmp/go-build /home/_/_/go/bin/go run . -tag bson,json aaa.sb` 通过。
  - 产物目录确认：`go/sb` 与 `ts/sb` 正常生成。
- **模板回归修复（RPC 参数与超时语义）**:
  - 修复 `ts.rpc.tpl` 参数序列化表达式：统一改为闭包式 setter 调用 `setXxx(buf, arg)`，覆盖基础类型、基础数组、枚举、枚举数组、结构体、结构体数组。
  - 修复 `i64/u64` 在 TS RPC 参数中的缺失 wrapper 问题：不再依赖 `_.i64/_.u64` 便捷函数，直接调用 `_.setI64/_.setU64`。
  - 修复 `go.rpc.tpl` 的响应体读取错误吞没：`io.ReadAll` 失败时返回 `RpcRespErr`。
  - 修复 Go Client 的超时语义：`Client.Timeout` 通过 `context.WithTimeout` 真实作用于每次请求；`HTTP` 默认改为无内置超时，避免与 `Client.Timeout` 双超时冲突。
- **入口清理（去除遗留死代码）**:
  - 删除 `main.go` 中已下线的 `writeDocs/buildDoc/docType` 及相关未使用导入，避免文档生成逻辑双轨维护。
- **测试补齐（tplgen 回归）**:
  - 新增 `internal/tplgen/template_regression_test.go`。
  - 用例1：校验 TS RPC 对 `u16[]/u64/i64/struct[]/bool` 参数均生成闭包式 `setXxx(buf, arg)`。
  - 用例2：校验 Go RPC 模板包含 `context.WithTimeout(ctx, c.Timeout)` 与 `ReadAll` 错误处理分支。
- **验证结果（本轮修复后）**:
  - `gofmt -w main.go internal/tplgen/template_regression_test.go` 已执行。
  - `GOCACHE=/tmp/go-build /home/_/_/go/bin/go test ./...` 通过。
  - 复现样例 `foo.test(nums [u16], id u64, v i64, ok bool) => nil` 重新生成后，`rpc.ts` 参数编码已修正为：`(buf) => _.setU16List(buf, nums)`、`(buf) => _.setU64(buf, id)`、`(buf) => _.setI64(buf, v)`。
- **TS 导入路径兼容性收敛（去除 .ts 扩展名）**:
  - 模板改动：`ts.rpc.tpl` 与 `ts.struct.tpl` 的 `import "./_.ts"` 改为 `import "./_"`。
  - 模板改动：`ts._.tpl` 的 `export "./type.ts"` 改为 `export "./type"`。
  - 生成器改动：`TsGenerator` 传给 `ts._.tpl` 的导出文件名统一为无扩展名（`enum`、`struct_xxx`），避免生成 `export * from "./enum.ts"` 之类路径。
  - 回归测试增强：`template_regression_test.go` 增加无扩展名导入/导出断言。
- **验证结果（TS 导入兼容性修复后）**:
  - `GOCACHE=/tmp/go-build /home/_/_/go/bin/go test ./...` 通过。
  - 实际生成确认：`rpc.ts` 首行已为 `import * as _ from "./_"`；`_.ts` 导出为 `export * from "./type"`、`export * from "./enum"`（无 `.ts` 后缀）。
- **方案2落地（业务入口枚举合法性校验）**:
  - 枚举模板增强：`go.enum.tpl` 为每个枚举生成 `IsValid()`，为枚举列表生成 `IsValid()`。
  - 结构体模板增强：`go.struct.tpl` 生成 `Validate()`，校验非零枚举字段、枚举列表、嵌套结构体/结构体列表；`Get/Set` 前后接入校验。
  - Handler 入口增强：`go.api._.tpl` 在 `parseRequest` 后增加参数校验：
    - 枚举参数：`!EnumType(v).IsValid()` 直接 `400`。
    - 枚举列表参数：`!v.IsValid()` 直接 `400`。
    - 结构体参数：`Validate()` 失败直接 `400`。
  - 修复模板回归：`IsValid` 的 `switch case` 改为每个 `case` 直接 `return true`，避免空 case 导致编译报 `missing return`。
- **验证结果（方案2）**:
  - `go run . -go ./go -ts ./ts -tag bson,json aaa.sb` 通过，产物已更新。
  - `go test ./...` 通过。
  - 样例验证：`SimOperator = Zz(2) | Lt(3)` 生成的 handler 中已包含：
    - `if !SimOperator(operator).IsValid() { ...400... }`
    - `if !operators.IsValid() { ...400... }`
    - `if err := (&sim).Validate(); err != nil { ...400... }`
- **枚举校验调用风格切换（方法式 -> 函数式）**:
  - 按约定将 `.IsValid()` 改为 `Is***Valid(e ***)`。
  - `go.enum.tpl` 改为生成：
    - `IsXxxValid(v Xxx) bool`
    - `IsXxxListValid(v XxxList) bool`
  - `go.struct.tpl` 的 `Validate()` 中枚举校验调用改为函数式。
  - `go.api._.tpl` 中入参校验调用改为函数式（标量枚举和枚举列表均已切换）。
  - 回归测试 `template_regression_test.go` 断言已同步为函数式命名。
- **验证结果（函数式 IsValid）**:
  - `go run . -go ./go -ts ./ts -tag bson,json aaa.sb` 通过。
  - `go test ./...` 通过。
  - 产物确认：`go/sb` 已无 `.IsValid()` 调用，存在 `IsSimOperatorValid(v SimOperator)` 与 `IsSimOperatorListValid(v SimOperatorList)`。
